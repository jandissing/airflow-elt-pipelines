version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: airflow-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-airflow_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-airflow_pass_2024}
      POSTGRES_DB: ${POSTGRES_DB:-airflow_db}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-airflow_user}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - airflow-network

  webserver:
    image: apache/airflow:3.3.0-python3.11
    container_name: airflow-webserver
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      AIRFLOW_HOME: /opt/airflow
      PYTHONPATH: /opt/airflow
      AIRFLOW__CORE__DAGS_FOLDER: /opt/airflow/dags
      AIRFLOW__CORE__LOAD_EXAMPLES: "False"
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: ${AIRFLOW__DATABASE__SQL_ALCHEMY_CONN}
      AIRFLOW__CORE__EXECUTOR: LocalExecutor
      AIRFLOW__CORE__FERNET_KEY: ${AIRFLOW__CORE__FERNET_KEY}
      AIRFLOW__WEBSERVER__SECRET_KEY: ${AIRFLOW__WEBSERVER__SECRET_KEY}
      AIRFLOW__API_AUTH__JWT_SECRET: "super_secret_jwt_key_for_airflow_3"
      AIRFLOW__LOGGING__LOG_SERVER_URL: "http://localhost:8793"
      AIRFLOW__SDK__LOG_SERVER_URL: "http://localhost:8793"
      _PIP_ADDITIONAL_REQUIREMENTS: "postgres psycopg2-binary openpyxl pandas sqlalchemy"
    volumes:
      - .:/opt/airflow
      - ./logs:/opt/airflow/logs
      - ./output:/opt/airflow/output
    ports:
      - "8080:8080"
    command: >
      bash -c "airflow db migrate &&
               airflow users create -r Admin -u admin -e admin@example.com -f Admin -l User -p admin 2>/dev/null || true &&
               airflow api-server"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/v2/monitor/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - airflow-network

  scheduler:
    image: apache/airflow:3.3.0-python3.11
    container_name: airflow-scheduler
    depends_on:
      webserver:
        condition: service_healthy
    environment:
      AIRFLOW_HOME: /opt/airflow
      PYTHONPATH: /opt/airflow
      AIRFLOW__CORE__DAGS_FOLDER: /opt/airflow/dags
      AIRFLOW__CORE__LOAD_EXAMPLES: "False"
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: ${AIRFLOW__DATABASE__SQL_ALCHEMY_CONN}
      AIRFLOW__CORE__EXECUTOR: LocalExecutor
      AIRFLOW__CORE__FERNET_KEY: ${AIRFLOW__CORE__FERNET_KEY}
      AIRFLOW__WEBSERVER__SECRET_KEY: ${AIRFLOW__WEBSERVER__SECRET_KEY}
      AIRFLOW__API_AUTH__JWT_SECRET: "super_secret_jwt_key_for_airflow_3"
      AIRFLOW__LOGGING__LOG_SERVER_URL: "http://localhost:8793"
      AIRFLOW__SDK__LOG_SERVER_URL: "http://localhost:8793"
      _PIP_ADDITIONAL_REQUIREMENTS: "postgres psycopg2-binary openpyxl pandas sqlalchemy"
    volumes:
      - .:/opt/airflow
      - ./logs:/opt/airflow/logs
      - ./output:/opt/airflow/output
    command: airflow scheduler
    networks:
      - airflow-network

volumes:
  postgres_data:

networks:
  airflow-network:
    driver: bridge
